{
  "name": "ionospheric-scintillation-monitor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Monorepo root for the ionospheric scintillation monitor (backend, client, dataProvider, frontend, teste)",
  "scripts": {
    "lint": "oxlint backend client dataProvider frontend teste oxlint.config.ts",
    "lint:fix": "oxlint --fix backend client dataProvider frontend teste oxlint.config.ts",
    "format": "oxfmt backend client dataProvider frontend teste oxfmt.config.ts oxlint.config.ts",
    "format:check": "oxfmt --check backend client dataProvider frontend teste oxfmt.config.ts oxlint.config.ts"
  },
  "devDependencies": {
    "oxfmt": "^0.70.0",
    "oxlint": "^1.85.0"
  }
}